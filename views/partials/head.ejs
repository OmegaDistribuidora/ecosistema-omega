<!DOCTYPE html>
<html lang="pt-BR">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="icon" type="image/png" href="<%= fixedLogoUrl || '/public/assets/omega-logo.svg' %>" />
    <link rel="shortcut icon" href="<%= fixedLogoUrl || '/public/assets/omega-logo.svg' %>" />
    <style>
      :root {
        --primary: <%= (theme && theme.primary_color) || '#03a9f4' %>;
        --secondary: <%= (theme && theme.secondary_color) || '#0d1b2a' %>;
        --accent: <%= (theme && theme.accent_color) || '#35d0a0' %>;
        --surface: <%= (theme && theme.surface_color) || '#111827' %>;
        --text: <%= (theme && theme.text_color) || '#f8fafc' %>;
      }
    </style>
    <link rel="stylesheet" href="/public/styles.css" />
  </head>
  <body>
